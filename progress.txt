## 2026-02-27 — US-001: Set up GSAP and global design tokens

**Status:** COMPLETE

**What was done:**
- Added `typecheck` (tsc --noEmit) and `test` scripts to package.json
- Installed `gsap@3.14.2` and `@gsap/react@2.1.2` via npm
- Rewrote `app/globals.css`: CSS custom properties (--color-bg, --color-text, --color-accent, --color-light, --color-muted), @theme inline block for Tailwind v4 token registration, html base styles (background-color, color, scroll-behavior: smooth, overflow-x: hidden)
- Updated `app/layout.tsx`: switched to DM Sans font via next/font/google, lang="en", body has bg-[#1A1A1A] text-[#F4F4F4]
- Emptied `app/page.tsx` to a single root `<div />` shell

**Next up:** US-002 (Sticky navigation bar) — create components/Nav.tsx as 'use client', fixed top nav with scroll behavior, active section tracking via IntersectionObserver, mobile hamburger menu.

## 2026-02-27 — US-002: Sticky navigation bar

**Status:** COMPLETE

**What was done:**
- Created `components/Nav.tsx` as a `'use client'` component
- Fixed top nav (`fixed top-0 left-0 right-0 z-50`) with site initials "ST" on the left and links (About, Experience, Projects, Resume, Contact) on the right
- Scroll listener: adds `bg-[#1A1A1A]/90 backdrop-blur-sm` after scrolling past 80px
- Active section highlighted with `text-[#FF4400]` via `IntersectionObserver` watching all 5 section IDs
- Mobile hamburger button (3 stacked lines) visible below `md` breakpoint; toggles a full-width dropdown; dropdown closes on link click
- Updated `app/page.tsx` to import and render `<Nav />`
- Typecheck passes, no tests configured

**Next up:** US-003 (Hero section) — create components/Hero.tsx with full-viewport layout, large headline, tagline, CTA buttons, and GSAP entrance animation.

## 2026-02-27 — US-003: Hero section

**Status:** COMPLETE

**What was done:**
- Created `components/Hero.tsx` as a `'use client'` component
- Full-viewport section (`min-h-screen flex items-center`) with `id='hero'`
- Headline "Simone Traversi" at `text-5xl md:text-7xl lg:text-9xl font-bold`
- Tagline "Full-Stack Developer & Creative Engineer" at `text-xl md:text-2xl text-[#757575]`
- Short positioning statement (2 lines) at `text-base md:text-lg text-[#D1E0E8]`
- CTA buttons: "View Work" (`bg-[#FF4400] text-[#F4F4F4]`) linking to `#projects`; "Get in Touch" (`border border-[#F4F4F4]`) linking to `#contact`
- Buttons `flex-col` on mobile, `flex-row gap-4` on `md+`
- GSAP entrance via `useGSAP`: all `.hero-animate` elements stagger from `y:60 opacity:0` on mount
- `gsap.registerPlugin(ScrollTrigger)` called at module level
- Updated `app/page.tsx` to import and render `<Hero />` below `<Nav />`
- Typecheck passes

**Next up:** US-004 (About section) — create components/About.tsx with two-column layout, bio text, trait bullets, image placeholder, and GSAP ScrollTrigger animation.

## 2026-02-27 — US-004: About section

**Status:** COMPLETE

**What was done:**
- Created `components/About.tsx` as a `'use client'` component
- Section with `id='about'` and `py-24 md:py-32 px-6` padding
- Heading "About" at `text-4xl md:text-6xl font-bold` with `border-b-2 border-[#FF4400] inline-block` underline accent
- Two-column `grid` on `md+`: left = `aspect-square bg-[#D1E0E8]/20` image placeholder, right = bio paragraph + 4 trait bullet points
- Bullet points use `text-[#FF4400]` for the `→` arrow symbol
- Single column on mobile; image on top, text below
- GSAP ScrollTrigger: container fades and slides up (`y:40 opacity:0 → y:0 opacity:1`) when entering the viewport
- `gsap.registerPlugin(ScrollTrigger)` called at module level
- Updated `app/page.tsx` to import and render `<About />` below `<Hero />`
- Typecheck passes

**Next up:** US-005 (Experience/Timeline section) — create components/Experience.tsx with vertical timeline, dot nodes, staggered GSAP ScrollTrigger animation.

## 2026-02-27 — US-005: Experience / Timeline section

**Status:** COMPLETE

**What was done:**
- Created `components/Experience.tsx` as a `'use client'` component
- Section with `id='experience'` and `py-24 md:py-32 px-6` padding
- Heading "Experience" at `text-4xl md:text-6xl font-bold`
- Vertical timeline: absolute left line (`left-1.5 w-px h-full bg-[#757575]`)
- Three placeholder entries: Acme Corp, Bright Digital, StartupXYZ
- Each entry has: `bg-[#FF4400] w-3 h-3 rounded-full` dot on the line, company name (`text-[#F4F4F4] font-semibold`), role title (`text-[#D1E0E8]`), date range (`text-[#757575] text-sm`), 2 bullet points (`text-[#D1E0E8] text-sm`)
- Mobile: `pl-10`; desktop: `md:pl-16`
- GSAP ScrollTrigger stagger: `.timeline-entry` elements animate from `x:-30 opacity:0` with `stagger:0.2` as section enters viewport
- `gsap.registerPlugin(ScrollTrigger)` called at module level
- Updated `app/page.tsx` to import and render `<Experience />` below `<About />`
- Typecheck passes

**Next up:** US-006 (Projects section) — create components/Projects.tsx with responsive grid, 3 project cards, tech tag pills, hover border effect, GSAP ScrollTrigger stagger animation.

## 2026-02-27 — US-006: Projects section

**Status:** COMPLETE

**What was done:**
- Created `components/Projects.tsx` as a `'use client'` component
- Section with `id='projects'` and `py-24 md:py-32 px-6` padding
- Heading "Projects" at `text-4xl md:text-6xl font-bold`
- Responsive grid: `grid-cols-1` (mobile) → `md:grid-cols-2` → `lg:grid-cols-3` with `gap-8`
- Three placeholder project cards: DataStream Platform, DesignKit UI, ShipFast CLI
- Each card has: `aspect-video bg-[#D1E0E8]/20` image placeholder, title (`text-[#F4F4F4] font-semibold text-xl`), description (`text-[#757575] text-sm line-clamp-2`), tech tag pills (`bg-[#1A1A1A] border border-[#757575] text-[#D1E0E8] text-xs px-2 py-1 rounded`), and `View →` link (`text-[#FF4400]`)
- Card hover: `border-[#757575]` → `hover:border-[#FF4400]` via `transition-colors duration-300`
- GSAP ScrollTrigger stagger: `.project-card` elements animate `y:40 opacity:0 → y:0 opacity:1` with `stagger:0.1`
- `gsap.registerPlugin(ScrollTrigger)` called at module level
- Updated `app/page.tsx` to import and render `<Projects />` below `<Experience />`
- Typecheck passes

**Next up:** US-007 (Resume section) — create components/Resume.tsx with download PDF button, skills block (3 groups × 4–5 tags), and education entry.

## 2026-02-27 — US-007: Resume section

**Status:** COMPLETE

**What was done:**
- Created `components/Resume.tsx` as a server component (no animations required)
- Section with `id='resume'` and `py-24 md:py-32 px-6` padding
- Heading "Resume" at `text-4xl md:text-6xl font-bold`
- Prominent `<a href="/resume.pdf" download>Download PDF</a>` button styled `bg-[#FF4400] text-[#F4F4F4] px-8 py-4 font-semibold`
- Skills block with 3 groups (Languages: TypeScript/JavaScript/Python/SQL/Rust, Frameworks: Next.js/React/Node.js/FastAPI/Tailwind CSS, Tools: Git/Docker/PostgreSQL/Redis/AWS), each group with skill tag pills (`bg-[#1A1A1A] border border-[#757575] text-[#D1E0E8] text-xs px-2 py-1 rounded`)
- Skill groups in `grid-cols-1` (mobile) → `md:grid-cols-3`
- Education block: University of Milan, B.Sc. Computer Science, 2018–2022 in `text-[#757575]`
- Created `public/resume.pdf` as a placeholder empty file
- Updated `app/page.tsx` to import and render `<Resume />` below `<Projects />`
- Typecheck passes

**Next up:** US-008 (Contact section) — create components/Contact.tsx with controlled form, social links, and GSAP ScrollTrigger heading animation.

## 2026-02-27 — US-008: Contact section

**Status:** COMPLETE

**What was done:**
- Created `components/Contact.tsx` as a `'use client'` component
- Section with `id='contact'` and `py-24 md:py-32 px-6` padding
- Heading "Let's work together." at `text-4xl md:text-6xl font-bold`
- Controlled form with Name (text), Email (email), Message (textarea rows=4); all inputs styled `bg-transparent border-b border-[#757575] text-[#F4F4F4] focus:border-[#FF4400] outline-none w-full py-3`
- Submit button `bg-[#FF4400] text-[#F4F4F4] px-8 py-4 font-semibold`
- On submit (preventDefault): button text changes to 'Message sent!' and form inputs cleared; no real API call
- Social links row (GitHub, LinkedIn, X/Twitter) with `href='#'` styled `text-[#757575] hover:text-[#FF4400]`
- Form is `max-w-xl mx-0` on desktop, full-width on mobile
- GSAP ScrollTrigger: heading animates from `y:40 opacity:0` on scroll
- `gsap.registerPlugin(ScrollTrigger)` called at module level
- Updated `app/page.tsx` to import and render `<Contact />` below `<Resume />`
- Typecheck passes

**Next up:** US-009 (Footer) — create components/Footer.tsx with copyright text and back-to-top link.

## 2026-02-27 — US-009: Footer

**Status:** COMPLETE

**What was done:**
- Created `components/Footer.tsx` as a server component (no client-side behavior needed)
- Footer with `border-t border-[#757575] py-8 px-6`
- Left side: copyright `© 2026 Simone Traversi` in `text-[#757575] text-sm`
- Right side: `Back to top ↑` link with `href='#hero'` styled `text-[#757575] hover:text-[#FF4400] text-sm transition-colors duration-200`
- Layout: `flex-col` on mobile; `md:flex-row md:justify-between` on md+
- Updated `app/page.tsx` to import and render `<Footer />` at the bottom
- Typecheck passes, no tests configured

**All US stories complete. PRD is fully implemented.**
